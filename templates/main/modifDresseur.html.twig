{% extends 'base.html.twig' %}

{% block title %}Modifier un dresseur{% endblock %}

{% block body %}


    <h3>Modifier un Dresseur</h3>

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_row(form.name) }}
    {#    {{ form_row(form.email) }} #}
    {{ form_row(form.password, {'attr': {'class': 'form'}}) }}
    {{ form_end(form) }}

{% endblock %}

